{
  "name": "behindthesite",
  "version": "1.0.0",
  "description": "Chrome extension showing company transparency information",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js",
    "migrate": "node scripts/migrate.js",
    "check": "npm run validate && npm run build"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/behindthesite/behindthesite"
  },
  "license": "MIT"
}
